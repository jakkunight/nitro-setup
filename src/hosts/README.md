# Hosts

This folder contains the hosts' configurations. Every host has a configuration
that describes its hardware they use/require to work.

Notice that only hardware and basic software configuration applies here. **DO
NOT PUT ARBITRARY PACKAGES THAT NO USER IN THE HOST WILL REQUIRE. ONLY PUT
SOFTWARE AND CONFIGURATIONS THAT EVERY USER IN THE HOST WILL USE.**
