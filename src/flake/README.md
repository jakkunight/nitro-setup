# Flake-Parts Utilities and modules

This folder contains all the `flake-parts` glue code logic for the Dendritic Nix
pattern. It also contains some helper functions to reduce some boilerplate.

It will allow the rest of the flake to work properly.
